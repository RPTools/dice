package net.rptools.maptool.dice.symbols;

/**
 * The valid types for variables and expression results for the dice expression evaluation.
 */
public enum DiceEvalType {
        INTEGER,
        DOUBLE,
        STRING,
        UNDEFINED
}
